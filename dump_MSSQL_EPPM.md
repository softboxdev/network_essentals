# Подробная инструкция по созданию новой базы EEPM из дампа 

## 📋 Что вам понадобится перед началом

1. **Файл дампа БД** (расширение `.bak`)
2. **Установленный Microsoft SQL Server** (любая версия)
3. **EEP Management Studio** с **Configuration Wizard**
4. **Права администратора** на компьютере

## 🔍 Шаг 1: Подготовка - находим нужные файлы и программы

### 1.1. Находим файл дампа
- Обычно имеет расширение `.bak`
- Может называться `eep_backup.bak`, `backup.bak` или подобное
- Запомните, где он лежит (например, `C:\Backups\eep_backup.bak`)

### 1.2. Находим EEPM Configuration Wizard
- Через меню Пуск → "EEP" → "Configuration Wizard"
- Или в папке установки EEP: `C:\Program Files\EEP\Configuration Wizard.exe`

### 1.3. Проверяем SQL Server
- Откройте **SQL Server Management Studio** (SSMS)
- Если нет - скачайте с официального сайта Microsoft

## 🗄️ Шаг 2: Восстанавливаем базу в SQL Server

### 2.1. Открываем SQL Server Management Studio

1. **Запустите SSMS** через Пуск
2. **Подключитесь к серверу**:
   - Server name: `localhost` или `.\SQLEXPRESS`
   - Authentication: "Windows Authentication"
3. **Нажмите "Connect"**

### 2.2. Восстанавливаем базу данных

1. **Щелкните правой кнопкой** на "Databases" в левом меню
2. **Выберите "Restore Database"**

![Шаг восстановления](https://i.imgur.com/restore-step.png)

3. **В открывшемся окне**:
   - В поле "To database" введите: `EEP_New_Database`
   - Выберите "From device"
   - Нажмите на кнопку "..." справа

4. **Выберите файл дампа**:
   - Нажмите "Add"
   - Найдите ваш файл `.bak`
   - Нажмите "OK"

5. **Настройте восстановление**:
   - Поставьте галочку напротив вашей базы в списке
   - Перейдите на вкладку "Options"
   - Поставьте галочку "Overwrite the existing database"
   - Проверьте пути к файлам (должны быть доступны)

6. **Нажмите "OK"** и ждите завершения

## 🛠️ Шаг 3: Настройка базы в EEPM Configuration Wizard

### 3.1. Запускаем Configuration Wizard

1. **Закройте все программы EEP** (если открыты)
2. **Запустите** "EEP Configuration Wizard"
3. **Выберите** "Create new configuration" или "New database"

### 3.2. Настраиваем подключение

1. **Database Server**: 
   - Введите `localhost` или имя вашего сервера SQL

2. **Authentication**:
   - Если используете Windows авторизацию - выберите "Windows Authentication"
   - Если отдельный логин - "SQL Server Authentication"

3. **Database Selection**:
   - Нажмите "Refresh" или "Browse"
   - Найдите в списке `EEP_New_Database` (которую восстановили)
   - Выберите её

### 3.3. Настраиваем параметры EEP

1. **System Settings**:
   - Company Name: введите название вашей организации
   - System Code: обычно оставляете по умолчанию

2. **Path Settings**:
   - Project Path: укажите папку для хранения проектов (например, `C:\EEP_Projects`)
   - Temp Path: папка для временных файлов

3. **User Administration**:
   - Создайте администратора системы:
     - Login: `admin`
     - Password: придумайте надежный пароль
     - User Name: `Administrator`

## ✅ Шаг 4: Проверяем установку

### 4.1. Тестовое подключение

1. **В Configuration Wizard** нажмите "Test Connection"
2. **Если все хорошо** - увидите сообщение "Connection successful"
3. **Если ошибка** - проверьте:
   - Запущен ли SQL Server
   - Правильно ли указано имя сервера
   - Есть ли права у пользователя

### 4.2. Запускаем EEP Management Studio

1. **Откройте EEP Management Studio**
2. **Подключитесь** к новой базе:
   - Выберите созданную конфигурацию
   - Введите логин/пароль администратора
3. **Проверьте**:
   - Открывается ли система
   - Есть ли данные из дампа
   - Работают ли основные функции

## 🚨 Возможные проблемы и решения

### ❌ Проблема: "Cannot connect to database"
**Решение**:
- Проверьте, запущена ли служба SQL Server
- В Services (службы) найдите "SQL Server (MSSQLSERVER)" и убедитесь, что статус "Running"

### ❌ Проблема: "Access denied"
**Решение**:
- Запустите программы от имени администратора
- Проверьте права пользователя в SQL Server

### ❌ Проблема: "Backup file is corrupted"
**Решение**:
- Проверьте целостность файла .bak
- Попробуйте восстановить из другой резервной копии

### ❌ Проблема: "Database already exists"
**Решение**:
- Удалите старую базу через SSMS или переименуйте новую

## 💾 Шаг 5: Финальные действия

1. **Создайте резервную копию** новой работающей системы
2. **Задокументируйте** параметры подключения
3. **Сообщите пользователям** данные для входа

## 📞 Если ничего не получается

1. **Обратитесь к администратору** вашей сети
2. **Проверьте логи** в папке Logs EEP
3. **Свяжитесь с технической поддержкой** EEP

---

**Важно**: Не удаляйте исходный файл дампа (.bak) до полной проверки работы новой системы!

Эта инструкция покрывает 95% типичных сценариев. Если столкнетесь с особой ошибкой - поищите её решение в документации к вашему конкретному EEP.

Инструкция по созданию новой базы данных в EEPM Configuration Wizard из дампа БД Microsoft SQL Server:

## Предварительные требования

1. **Исходный дамп БД** в формате `.bak` (резервная копия SQL Server)
2. **Учетные записи** с правами администратора для SQL Server
3. **EEP Management Studio** и **Configuration Wizard**
4. **Свободное место** на диске для новой БД

## Пошаговая инструкция

### 1. Подготовка в SQL Server Management Studio

```sql
-- Проверка существующих баз данных
SELECT name FROM sys.databases;

-- Восстановление базы данных из дампа
RESTORE DATABASE [Новая_База_EEP]
FROM DISK = 'C:\Путь\К\Дампу\eep_backup.bak'
WITH 
MOVE 'EEP_Data' TO 'C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA\Новая_База_EEP.mdf',
MOVE 'EEP_Log' TO 'C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA\Новая_База_EEP.ldf',
REPLACE, STATS = 5;
```

### 2. Запуск EEPM Configuration Wizard

1. Откройте **EEP Management Studio**
2. Запустите **Configuration Wizard** 
3. Выберите **"Create new database"**

### 3. Настройка подключения к БД

В Configuration Wizard:

1. **Server Name**: Укажите имя SQL Server экземпляра
2. **Authentication**: Выберите тип аутентификации
   - Windows Authentication
   - SQL Server Authentication
3. **Database**: Выберите восстановленную базу "Новая_База_EEP"

### 4. Конфигурация параметров БД

```sql
-- Проверка и настройка параметров БД
ALTER DATABASE [Новая_База_EEP] SET RECOVERY SIMPLE;
ALTER DATABASE [Новая_База_EEP] SET MULTI_USER;
```

### 5. Настройка прав доступа

```sql
-- Создание пользователя для EEP (если требуется)
CREATE LOGIN [eep_user] WITH PASSWORD = 'Ваш_Пароль';
USE [Новая_База_EEP];
CREATE USER [eep_user] FOR LOGIN [eep_user];
EXEC sp_addrolemember 'db_owner', 'eep_user';
```

### 6. Завершение настройки в Configuration Wizard

1. **Проверка подключения** - тест соединения с БД
2. **Настройка параметров EEP** - укажите основные настройки системы
3. **Создание конфигурационных файлов**
4. **Завершение мастера**

### 7. Проверка работоспособности

1. **Подключитесь к новой БД** через EEP Management Studio
2. **Проверьте основные модули** системы
3. **Убедитесь в целостности данных** из дампа

## Важные замечания

- **Версия SQL Server** должна быть совместима с исходной БД
- **Версия EEP** должна поддерживать структуру восстановленной БД
- **Перед восстановлением** создайте резервную копию существующих данных
- **Проверьте совместимость** уровней базы данных

## Типичные проблемы и решения

1. **Ошибки прав доступа** - проверьте учетные записи SQL Server
2. **Несовместимость версий** - обновите EEP или SQL Server
3. **Поврежденный дамп** - проверьте целостность файла .bak

После выполнения этих шагов у вас будет полностью функционирующая новая база данных EEP на основе существующего дампа.