# 🔢 Как сравнивать числа в Bash

Для сравнения **чисел** в Bash используются **специальные операторы** — `-eq`, `-ne`, `-gt`, `-lt`, `-ge`, `-le`. Их нельзя заменять на `=` или `==`!

---

## 📌 Основные операторы для чисел

| Оператор | Значение | Английский | Пример |
|----------|----------|------------|--------|
| `-eq` | равно | **eq**ual | `[ "$a" -eq "$b" ]` |
| `-ne` | не равно | **n**ot **e**qual | `[ "$a" -ne "$b" ]` |
| `-gt` | больше | **g**reater **t**han | `[ "$a" -gt "$b" ]` |
| `-ge` | больше или равно | **g**reater or **e**qual | `[ "$a" -ge "$b" ]` |
| `-lt` | меньше | **l**ess **t**han | `[ "$a" -lt "$b" ]` |
| `-le` | меньше или равно | **l**ess or **e**qual | `[ "$a" -le "$b" ]` |

**Мнемоника:**
- `-eq` → **eq**ual
- `-ne` → **n**ot **e**qual
- `-gt` → **g**reater **t**han
- `-lt` → **l**ess **t**han
- `-ge` → **g**reater or **e**qual
- `-le` → **l**ess or **e**qual

---

## 🧪 Примеры

### 1. Проверка на равенство

```bash
a=5
b=5

if [ "$a" -eq "$b" ]; then
    echo "Числа равны"
fi
# Вывод: Числа равны
```

### 2. Проверка на неравенство

```bash
a=5
b=3

if [ "$a" -ne "$b" ]; then
    echo "Числа не равны"
fi
# Вывод: Числа не равны
```

### 3. Больше / меньше

```bash
a=10
b=7

if [ "$a" -gt "$b" ]; then
    echo "$a больше $b"
fi

if [ "$a" -lt "$b" ]; then
    echo "$a меньше $b"
fi
# Вывод: 10 больше 7
```

### 4. Больше или равно / меньше или равно

```bash
a=5
b=5

if [ "$a" -ge "$b" ]; then
    echo "$a больше или равно $b"
fi

if [ "$a" -le "$b" ]; then
    echo "$a меньше или равно $b"
fi
# Вывод: 5 больше или равно 5
#        5 меньше или равно 5
```

---

## ⚠️ Почему нельзя использовать `=` и `==`

### Опасность №1: `=` сравнивает **строки**, а не числа

```bash
a=5
b=05

[ "$a" = "$b" ]     # ЛОЖЬ — "5" ≠ "05" (строки разные!)
[ "$a" -eq "$b" ]   # ИСТИНА — 5 = 5 (числа равны)
```

**Что произошло?**
- Строковое сравнение: символ `5` не равен строке `05`
- Числовое сравнение: математически 5 и 05 — одно и то же число

### Опасность №2: `=` ломается на лидирующих нулях

```bash
a=08

[ "$a" = 8 ]     # ЛОЖЬ — строки "08" и "8" разные
[ "$a" -eq 8 ]   # ИСТИНА — 8 = 8
```

### Опасность №3: `==` тоже строковое

```bash
[ "10" == "9" ]    # ЛОЖЬ — строка "10" ≠ "9"
[ "10" -gt "9" ]   # ИСТИНА — 10 > 9
```

Если бы мы сравнивали как строки, то `"10"` **меньше** `"9"`, потому что символ `1` идёт раньше `9` в алфавите. Но как числа `10 > 9`.

---

## 🎯 Сравнение чисел: `[ ]` vs `(( ))`

### Способ 1: `[ ... ]` с операторами `-eq`, `-lt`, ...

```bash
a=10
b=5

if [ "$a" -gt "$b" ]; then
    echo "a больше b"
fi
```

**Особенности:**
- Работает в любом POSIX-совместимом shell
- Требует кавычек вокруг переменных
- Использует **буквенные** операторы (`-gt`, `-lt`)

### Способ 2: `(( ... ))` с математическими символами

```bash
a=10
b=5

if (( a > b )); then
    echo "a больше b"
fi
```

**Особенности:**
- **Только Bash** (не POSIX)
- Кавычки **не нужны** (и даже вредны)
- Использует **привычные** символы: `>`, `<`, `>=`, `<=`, `==`, `!=`
- Внутри можно писать **арифметические выражения** напрямую

---

## 📊 Сравнение двух способов

| Операция | `[ ]` | `(( ))` |
|----------|-------|---------|
| равно | `[ "$a" -eq "$b" ]` | `(( a == b ))` |
| не равно | `[ "$a" -ne "$b" ]` | `(( a != b ))` |
| больше | `[ "$a" -gt "$b" ]` | `(( a > b ))` |
| больше или равно | `[ "$a" -ge "$b" ]` | `(( a >= b ))` |
| меньше | `[ "$a" -lt "$b" ]` | `(( a < b ))` |
| меньше или равно | `[ "$a" -le "$b" ]` | `(( a <= b ))` |
| есть кавычки | ✅ нужны | ❌ не нужны |
| POSIX | ✅ да | ❌ только Bash |
| Арифметика внутри | ❌ нет | ✅ да |

---

## 💡 Пример: `(( ))` с арифметикой

```bash
a=5
b=3

if (( a * 2 > b + 5 )); then
    echo "5*2 больше 3+5"
fi
# Вывод: 5*2 больше 3+5
```

С `[ ]` пришлось бы писать так:

```bash
if [ $((a * 2)) -gt $((b + 5)) ]; then
    echo "5*2 больше 3+5"
fi
```

Согласитесь, `(( ))` читается проще!

---

## 🔬 Практический пример: сравнение с пользовательским вводом

```bash
#!/bin/bash
read -p "Введите число: " num

# Способ 1: через [ ]
if [ "$num" -gt 100 ]; then
    echo "Число больше 100"
elif [ "$num" -eq 100 ]; then
    echo "Число равно 100"
else
    echo "Число меньше 100"
fi

# Способ 2: через (( ))
if (( num > 100 )); then
    echo "Число больше 100"
elif (( num == 100 )); then
    echo "Число равно 100"
else
    echo "Число меньше 100"
fi
```

Оба варианта работают одинаково. Выбирайте по вкусу.

---

## ⚠️ Частые ошибки

### ❌ Ошибка 1: использование `>` и `<` внутри `[ ]`

```bash
[ "$a" > "$b" ]      # ❌ НЕПРАВИЛЬНО!
```

**Что произойдёт?** Bash воспримет `>` как **перенаправление вывода**, а не как сравнение! Создастся файл с именем `$b`, а результат будет непредсказуемым.

```bash
[ "$a" -gt "$b" ]    # ✅ ПРАВИЛЬНО
(( a > b ))          # ✅ ТОЖЕ ПРАВИЛЬНО
```

### ❌ Ошибка 2: сравнение нечисловых строк

```bash
a="abc"
[ "$a" -gt 5 ]    # ❌ ОШИБКА: "abc" не число
```

Вывод:
```
bash: [: abc: integer expression expected
```

Перед сравнением чисел **всегда проверяйте**, что это числа:

```bash
if [[ "$a" =~ ^-?[0-9]+$ ]] && [ "$a" -gt 5 ]; then
    echo "a больше 5"
fi
```

### ❌ Ошибка 3: забыли кавычки

```bash
a=""
[ $a -eq 5 ]     # ❌ ОШИБКА: [ -eq: unary operator expected
[ "$a" -eq 5 ]   # ⚠️ тоже ошибка, но другая: integer expression expected
```

Всегда используйте кавычки в `[ ]`:

```bash
[ "$a" -eq 5 ]   # ✅
```

### ❌ Ошибка 4: смешение типов операторов

```bash
[ "$a" -eq "$b" ]    # ✅ числа
[ "$a" = "$b" ]      # ✅ строки
[ "$a" == "$b" ]     # ✅ строки (bash)
[ "$a" -eq "$b" ]    # ✅ числа
[ "$a" -e "$b" ]     # ❌ такой операции не существует
```

---

## 📋 Шпаргалка

```
┌─────────────────────────────────────────────────────────┐
│              СРАВНЕНИЕ ЧИСЕЛ В BASH                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [ "$a" -eq "$b" ]    равно                             │
│  [ "$a" -ne "$b" ]    не равно                          │
│  [ "$a" -gt "$b" ]    больше                            │
│  [ "$a" -ge "$b" ]    больше или равно                  │
│  [ "$a" -lt "$b" ]    меньше                            │
│  [ "$a" -le "$b" ]    меньше или равно                  │
│                                                         │
│  ─── Bash-альтернатива ───                              │
│                                                         │
│  (( a == b ))         равно                             │
│  (( a != b ))         не равно                          │
│  (( a > b ))          больше                            │
│  (( a >= b ))         больше или равно                  │
│  (( a < b ))          меньше                            │
│  (( a <= b ))         меньше или равно                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎓 Итог

1. **Для чисел** используйте `-eq`, `-ne`, `-gt`, `-lt`, `-ge`, `-le` — **не** `=` или `==`
2. **Для строк** используйте `=` (POSIX) или `==` (Bash)
3. **`>` и `<`** внутри `[ ]` **не работают** — используйте `-gt` / `-lt`
4. **`(( ))`** — удобная Bash-альтернатива с привычными символами `>`, `<`, `==`
5. **Всегда проверяйте**, что переменные содержат числа перед сравнением
6. **Всегда ставьте кавычки** в `[ ]`: `[ "$a" -eq "$b" ]`

